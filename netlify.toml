[build]
  command = "cd artifacts/mockup-sandbox && rm -rf node_modules package-lock.json && npm install && npm run build"
  publish = "artifacts/mockup-sandbox/dist"

[build.environment]
  NODE_VERSION = "22"